# GLOM Auth API (No Database Version)

API بسيط لتسجيل الدخول وربط الحساب مع HWID لتويك GLOM، بدون استخدام أي قاعدة بيانات.
يستخدم ملف `users.json` فقط لتخزين المستخدمين و الـ HWID.

> هذا المشروع مناسب لرفعه على GitHub ثم استضافته على أي خدمة Node.js (مثلاً Render Web Service أو Railway أو غيرها).

---

## 📂 محتويات المشروع

- `index.js` → ملف الـ API الرئيسي (Express)
- `users.json` → بيانات المستخدمين (تقدر تعدلها يدويًا)
- `package.json` → معلومات المشروع + المكتبات
- `.gitignore` → تجاهل مجلد `node_modules` عند رفعه على GitHub

---

## 🔧 طريقة التشغيل محليًا

1. تأكد أن عندك Node.js مثبت.
2. افتح مجلد المشروع في الطرفية (Terminal) ثم نفّذ:

```bash
npm install
npm start
```

3. بيشتغل الـ API على المنفذ `3000` (أو أي منفذ في `PORT`).

---

## 🌐 المسارات (Endpoints)

### 1) فحص الحالة

**GET** `/health`

الرد:

```json
{
  "status": "ok",
  "message": "GLOM Auth API is running"
}
```

---

### 2) تسجيل الدخول + التحقق من HWID

**POST** `/login`

Body (JSON):

```json
{
  "username": "demo",
  "password": "demo123",
  "hwid": "HWID_FROM_CLIENT"
}
```

#### الردود المحتملة:

- ✅ أول تسجيل دخول (يربط الـ HWID لأول مرة):

```json
{
  "success": true,
  "code": "HWID_REGISTERED",
  "message": "First login on this account, HWID registered"
}
```

- ✅ تسجيل دخول ناجح والـ HWID مطابق:

```json
{
  "success": true,
  "code": "OK",
  "message": "Login successful"
}
```

- ❌ بيانات دخول خاطئة:

```json
{
  "success": false,
  "code": "INVALID_CREDENTIALS",
  "message": "Invalid username or password"
}
```

- ❌ بدون HWID:

```json
{
  "success": false,
  "code": "HWID_REQUIRED",
  "message": "HWID is required"
}
```

- ❌ الحساب مربوط بجهاز آخر:

```json
{
  "success": false,
  "code": "HWID_MISMATCH",
  "message": "This account is already bound to another device"
}
```

---

## 👤 المستخدم الافتراضي

في ملف `users.json` موجود مستخدم تجريبي:

```json
{
  "username": "demo",
  "password": "demo123",
  "hwid": "",
  "isAdmin": true
}
```

تقدر تغيّر أو تضيف مستخدمين يدويًا في نفس الملف قبل التشغيل.

---

## 📌 ملاحظات مهمة

- هذا المشروع بسيط ومقصود للاستخدام مع عدد محدود من المستخدمين.
- لو صار عندك عدد مستخدمين كبير، الأفضل تستخدم قاعدة بيانات حقيقية.
- لا تخزّن كلمات السر الحقيقية لعملائك هنا بدون تشفير.
